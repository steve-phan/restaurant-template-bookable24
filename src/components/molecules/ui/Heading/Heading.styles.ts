import { styled } from '@mui/material/styles';

export const HeadingCenterSt = styled('span')(({ theme }) => ({
  display: 'block',
  fontFamily: 'Work Sans,sans-serif',
  fontSize: 16,
  textAlign: 'center',
  fontWeight: 700,
}));

export const EmptyViewCartNoticeSt = styled('span')(({ theme }) => ({
  textAlign: 'center',
}));
